[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "worker-agent"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "langgraph",
    "google-genai",
    "python-dotenv",
]
